{
    "builds": [{
        "src": "app.py",
        "use": "@vercel/python"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "app.py"
    }],
    "env": {
        "DATABASE_URL": "@database_url",
        "JWT_SECRET_KEY": "@jwt_secret_key"
    }
}